apiVersion: v1
kind: Service
metadata:
  name: {{ service_name }}
  namespace: {{ namespace }}
  labels:
    app: linkedin-bot
    session-id: {{ session_id }}
    user-id: {{ user_id }}
  annotations:
    description: "LinkedIn bot service"
    created-by: "Admin Bot System"
spec:
  selector:
    app: linkedin-bot
    session-id: {{ session_id }}
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP 